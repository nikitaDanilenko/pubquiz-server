apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Release.Name }}-strip-api
  labels:
    {{- include "pubquiz-server.labels" . | nindent 4 }}
spec:
  stripPrefix:
    prefixes:
      - /api
